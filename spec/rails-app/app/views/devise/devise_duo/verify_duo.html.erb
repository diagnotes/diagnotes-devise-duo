<h2>
  <%= I18n.t('duo_register_title', {:scope => 'devise'}) %>
</h2>

<%= verify_duo_form do %>
  <legend><%= I18n.t('submit_token_title', {:scope => 'devise'}) %></legend>
  <%= label_tag :token %>
  <%= text_field_tag :token, "", :autocomplete => :off, :id => 'duo-token' %>
  <%= link_to '?', '#', :id => 'duo-help' %>
  <%= duo_request_sms_link %>
  <%= submit_tag I18n.t('submit_token', {:scope => 'devise'}), :class => 'btn' %>
<% end %>

